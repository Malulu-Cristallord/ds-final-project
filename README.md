# ds-final-project
Set you SerpApi key in environment variables before using.

Search -> Edit the system environment variables -> Environment Variables... -> User variables for user -> New -> (variable name: SERP_API_KEY)